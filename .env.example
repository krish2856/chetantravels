# Database Configuration
# Option 1: Full PostgreSQL connection string (e.g. Supabase, local PostgreSQL, etc.)
# DATABASE_URL=postgresql://postgres:password@localhost:5432/notherbooking

# Option 2: Individual connection parameters
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=postgres
DB_NAME=notherbooking

# Server Configuration
PORT=3000
NODE_ENV=development
